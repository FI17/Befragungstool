
@model ICollection<QuestionViewModel>

@if (Model != null)
{
    <table>
        @{ 
            Session["Maximum"] = Model.Count()-1;
        }

        @for (int i = 0; i < Model.Count(); i++)
        {
            @Html.Action("Vorschau_Fragentyp", Model.ToList()[i])
        }
    </table>
}
